version: 2.1

jobs:
  build:
    docker:
      - image: misterabdul/android-build-environment:0.0.1-alpine
    steps:
      - checkout
      - run: |
          ./gradlew init
          ./gradlew :app:bundleRelease
          cp app/build/outputs/bundle/release /tmp/artifacts
      - store_artifacts:
          path: /tmp/artifacts

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
